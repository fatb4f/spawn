openapi: 3.1.0
info:
  title: spawn control and event contracts
  version: 0.1.0
  description: |
    Canonical API surface for spawn control-plane and contract references.
servers: []
paths: {}
components:
  schemas:
    EventEnvelopeV1:
      $ref: "./schemas/EventEnvelopeV1.schema.json"
    ActionRequestV1:
      $ref: "./schemas/ActionRequestV1.schema.json"
    ActionResultV1:
      $ref: "./schemas/ActionResultV1.schema.json"
